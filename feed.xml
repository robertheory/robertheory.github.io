<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="pt-BR"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://robertheory.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://robertheory.github.io/" rel="alternate" type="text/html" hreflang="pt-BR"/><updated>2025-04-26T00:00:53+00:00</updated><id>https://robertheory.github.io/feed.xml</id><title type="html">Roberto Costa</title><subtitle>Desenvolvedor de software com mais de 10 anos de experiência, especializado em desenvolvimento full-stack, arquitetura de software e liderança de projetos, atuando principalmente em startups. </subtitle><entry><title type="html">Redux 101 - gerenciadores de estados simplificados</title><link href="https://robertheory.github.io/blog/2024/redux-101/" rel="alternate" type="text/html" title="Redux 101 - gerenciadores de estados simplificados"/><published>2024-01-03T13:00:00+00:00</published><updated>2024-01-03T13:00:00+00:00</updated><id>https://robertheory.github.io/blog/2024/redux-101</id><content type="html" xml:base="https://robertheory.github.io/blog/2024/redux-101/"><![CDATA[<p>Estados complexos são um desafio, então você decide usar Redux, mas encontra outro desafio: <strong><em>entender o Redux</em></strong>. Mas hoje vamos ver tudo que você precisa saber para começar bem com essa ferramenta!</p> <h2 id="table-of-contents">Table of contents</h2> <ul> <li><a href="#o-que-e-o-redux">O que é o Redux</a></li> <li><a href="#quando-devo-utilizar">Quando devo utilizar</a></li> <li><a href="#quando-não-devo-utilizar">Quando não devo utilizar</a></li> <li><a href="#como-funciona-o-redux">Como funciona o Redux</a> <ul> <li><a href="#arquitetura-flux">Arquitetura Flux</a></li> <li><a href="#mergulhando-no-redux">Mergulhando no Redux</a> <ul> <li><a href="#slices">Slices</a></li> <li><a href="#os-reducers">Os Reducers</a></li> </ul> </li> </ul> </li> <li><a href="#mão-na-massa">Mão na massa!</a> <ul> <li><a href="#funcionalidades">Funcionalidades</a></li> <li><a href="#implementação">Implementação</a> <ul> <li><a href="#1-preparação">1 Preparação</a></li> <li><a href="#2-criando-a-store">2 Criando a Store</a></li> <li><a href="#3-configurando-o-provider">3 Configurando o Provider</a></li> <li><a href="#4-slices">4 Slices</a></li> <li><a href="#5-reducers">5 Reducers</a></li> </ul> </li> <li><a href="#ligado-os-dados-aos-componentes">Ligado os dados aos componentes!</a></li> </ul> </li> <li><a href="#persistindo-dados">Persistindo dados</a> <ul> <li><a href="#seletores-personalizados">Seletores personalizados</a></li> </ul> </li> <li><a href="#redux-com-nextjs">Redux com NextJs</a></li> <li><a href="#referencias">Referências</a></li> </ul> <h2 id="o-que-é-o-redux-">O que é o Redux <a name="o-que-e-o-redux"></a></h2> <p>Basicamente, Redux é uma solução para gerenciar estados, ele brilha de verdade quando temos que lidar com casos mais complexos como estados distribuídos e <a href="https://www.alura.com.br/artigos/prop-drilling-no-react-js">Prop Drilling</a>.</p> <p>De acordo com a sua <a href="https://redux.js.org">documentação</a>:</p> <blockquote> <p>O Redux é um contêiner de estado previsível para aplicativos JavaScript. Ele ajuda a escrever aplicativos que se comportam de forma consistente, são executados em diferentes ambientes (cliente, servidor e nativo) e são fáceis de testar. Além disso, ele fornece uma ótima experiência de desenvolvedor, como a edição de código ao vivo combinada com um depurador que viaja no tempo.</p> </blockquote> <p>E realmente, essa biblioteca é bem versátil em trabalhar com outras bibliotecas e frameworks de visualização, tornando uma ótima escolha para trabalhar com gerenciamento de estados.</p> <p>Contudo, é muito fácil fazer um uso equivocado desta ferramenta, devido a sua popularidade e praticidade, muitos projetos adicionam o Redux automaticamente sem refletir se realmente precisam disso.</p> <h2 id="quando-devo-utilizar-">Quando devo utilizar <a name="quando-devo-utilizar"></a></h2> <p>Como muitas coisas no mundo da tecnologia, o melhor uso do Redux vem quando você percebe sua necessidade na aplicação, e alguns sinais disso são:</p> <ul> <li> <p><strong>Complexidade do Estado</strong>: Se sua aplicação tem um estado complexo, com múltiplos componentes que precisam acessar e modificar esse estado, o Redux pode fornecer uma maneira mais organizada de gerenciar o estado global.</p> </li> <li> <p><strong>Compartilhamento de Estado</strong>: Quando há a necessidade de compartilhar estado entre componentes que não têm uma relação pai-filho direta, o Redux oferece uma solução centralizada para acessar e modificar o estado.</p> </li> </ul> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ruhn99wnz9nzmn8iwrbq.png" alt="imagem de Componentes distantes"/></p> <ul> <li><strong>Prop Drilling</strong>: Quando existe uma relação pai-filho, mas para passar o estado para o filho desejado você precisa passar por vários componentes filhos até chegar nele.</li> </ul> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/f33vgxvdicqeq51naebs.png" alt="imagem de Prop Drilling"/></p> <h2 id="quando-não-devo-utilizar-">Quando não devo utilizar <a name="quando-nao-devo-utilizar"></a></h2> <ul> <li> <p><strong>Aplicação pequena ou simples</strong>: Se sua aplicação é pequena e possui requisitos de gerenciamento de estado simples, a introdução do Redux pode ser excessiva.</p> </li> <li> <p><strong>Curva de Aprendizado da Equipe</strong>: Se sua equipe não tem familiaridade com o Redux e a curva de aprendizado é um fator crítico, pode ser mais sensato evitar a introdução desta ferramenta, especialmente se os benefícios esperados não justificarem o tempo de aprendizado.</p> </li> <li> <p><strong>Ferramentas Alternativas</strong>: Existem várias alternativas no que se diz respeito a gerenciar estados no ReactJS, dentre elas podemos citar:</p> </li> <li><a href="https://github.com/pmndrs/zustand">Zustand</a></li> <li><a href="https://recoiljs.org">Recoil</a></li> <li><a href="https://mobx.js.org">MobX</a></li> </ul> <p>Mas existe algo melhor ainda: <a href="https://react.dev/reference/react/createContext">React Context</a> e <a href="https://react.dev/reference/react/useReducer">useReducer hook</a></p> <p>Nada melhor do que usar as ferramentas nativas do próprio ReactJS para resolver as questões que temos, assim podemos depender de menos bibliotecas externas e tornando nosso projeto mais simples.</p> <p>Apesar desta abordagem ser algo que eu destaco como ideal, ou pelo menos mais interassante, é assunto para outro artigo, já que hoje vamos falar de Redux.</p> <h2 id="como-funciona-o-redux-">Como funciona o Redux <a name="como-funciona-o-redux"></a></h2> <h3 id="arquitetura-flux-">Arquitetura Flux <a name="arquitetura-flux"></a></h3> <p>A arquitetura do <a href="https://facebookarchive.github.io/flux/">Flux</a> foi proposta pelo Facebook para construção de SPAs que divide a aplicação nas seguintes partes:</p> <ul> <li><strong>View</strong>: um componente React, ou Angular, etc;</li> <li><strong>Action</strong>: um evento que descreve a mudança que está ocorrendo e carrega os dados necessários;</li> <li><strong>Dispatcher</strong>: responsável por levar os eventos das actions até a <code class="language-plaintext highlighter-rouge">store</code> responsável;</li> <li><strong>Store</strong>: recebe os eventos e lida com as mudanças de estado.</li> </ul> <p>Importante ressaltar que:</p> <ul> <li>O estado é imutável, logo não pode ser modificado diretamente;</li> <li>A única forma de atualizar o estado é através das actions e dispatchers</li> </ul> <p>Criei um exemplo visual para entendermos como isso funciona:</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/raocz91t22byxbsqf5r9.png" alt="arquitura flux"/></p> <p>Neste exemplo, temos uma <code class="language-plaintext highlighter-rouge">store</code> de carrinho de compras chamada <code class="language-plaintext highlighter-rouge">carrinho</code> e algumas <code class="language-plaintext highlighter-rouge">actions</code> que representam operações no carrinho.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/lvllu743p9k2hdr5ws1t.png" alt="exemplo store flux"/></p> <p>Logo depois vamos usar nos nossos componentes React:</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ctzbs14t5q26nkwps420.png" alt="implementação do flux"/></p> <ul> <li>O componente <code class="language-plaintext highlighter-rouge">Carrinho</code> está observando a <code class="language-plaintext highlighter-rouge">store</code>, que contém a lista de produtos do carrinho;</li> <li>O componente <code class="language-plaintext highlighter-rouge">Produto</code> , bem distante do carrinho, vai acionar a <code class="language-plaintext highlighter-rouge">action</code> chamada <code class="language-plaintext highlighter-rouge">adicionar ao carrinho</code>;</li> <li>A <code class="language-plaintext highlighter-rouge">action</code> vai ser recebida pelo <code class="language-plaintext highlighter-rouge">dispatcher</code> que vai interagir com a <code class="language-plaintext highlighter-rouge">store</code>;</li> <li>A <code class="language-plaintext highlighter-rouge">store</code> recebe a <code class="language-plaintext highlighter-rouge">action</code> e atualiza seu estado com o novo produto;</li> <li>O carrinho recebe o novo estado e se atualiza.</li> </ul> <p>Se você entendeu este fluxo, praticamente entendeu toda a lógica maior do Redux, pois ele segue fielmente esta arquitetura!</p> <h3 id="mergulhando-no-redux-">Mergulhando no Redux <a name="mergulhando-no-redux"></a></h3> <p>Agora que já falamos sobre a arquitetura por trás de tudo, vamos ver como que o Redux funciona de fato. Vamos ver alguns termos e conceitos a se familiarizar na prática desta ferramenta.</p> <h4 id="slices-">Slices <a name="slices"></a></h4> <p>É importante notar que a store é um estado “global” que contém outros estados, frequentemente chamados de “fatias” ou <code class="language-plaintext highlighter-rouge">slices</code>, então o nosso estado de carrinho é um <code class="language-plaintext highlighter-rouge">slice</code>.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ttf17sg504p8gyli7eyx.png" alt="Redux Store"/></p> <h4 id="os-reducers-">Os Reducers <a name="os-reducers"></a></h4> <p>Temos um novo jogador no campo, os reducers são funções super simples que são utilizadas para atualizar o estado da aplicação. Eles tem responsabilidades e escopos muito bem definidos, assim como as actions:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>function addToCart(state, action) {  
  return {
     cart: [...state.cart, action.product]
  }
}
</code></pre></div></div> <h2 id="mão-na-massa-">Mão na massa! <a name="mao-na-massa-"></a></h2> <p>Nada melhor do que um exemplo de código para entendermos como ligar nossa aplicação ReactJS com o Redux.</p> <p>Para este propósito fiz o seguinte projeto didático: <a href="https://github.com/robertheory/react-state-management">React State Management 101</a></p> <p>Essa demo é uma loja fictícia onde temos duas funcionalidades básicas para atender: gerenciar favoritos e carrinho de compras.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/xbstv5f3kcox4g4k0n1r.png" alt="print de tela"/></p> <p>Como nosso intuito aqui é a implementação do Redux, não vamos focar em detalhes como estilização..</p> <h3 id="funcionalidades-">Funcionalidades <a name="funcionalidades"></a></h3> <p>Antes mesmo de colocar a mão no código, precisamos entender as funcionalidades pretendidas nesta aplicação:</p> <ul> <li>Favoritar Adiciona o item à lista de favoritos. Caso este item já seja um favorito, ele será removido da lista.</li> <li>Adicionar ao carrinho Adiciona este item ao carrinho. Caso este item já esteja no carrinho, sua quantidade deve ser incrementada.</li> <li>Remover do carrinho Decrementa a quantidade do item no carrinho. Caso o item tenha apenas 1 de quantidade, é removido do carrinho.</li> <li>Deletar do carrinho Remove completamente o item do carrinho.</li> </ul> <h3 id="implementação-">Implementação <a name="implementacao"></a></h3> <p>Entraremos agora em detalhes de implementação do projeto, contudo não vou demonstrar todas as funcionalidades, apenas a de favoritar para simplificar este exemplo.</p> <h4 id="1-preparação-">1 Preparação <a name="1-preparacao"></a></h4> <p>Começando do zero em um projeto ReactJS recém criado, execute:</p> <p><code class="language-plaintext highlighter-rouge">yarn add @reduxjs/toolkit</code> ou <code class="language-plaintext highlighter-rouge">npm install @reduxjs/toolkit</code></p> <p>Tendo em vista a atual implementação recomendada na documentação do <a href="https://redux.js.org/">Redux</a>, temos uma maneira mais interessante de seguir usando ReactJS: vamos utilizar o <a href="https://react-redux.js.org/">React-Redux</a>, uma forma de interagir melhor com os componentes React usando todo o poder da biblioteca original.</p> <p><code class="language-plaintext highlighter-rouge">yarn add react-redux</code> ou <code class="language-plaintext highlighter-rouge">npm install react-redux</code></p> <p>Agora estamos prontos para o primeiro passo: criar a nossa Store!</p> <h4 id="2-criando-a-store-">2 Criando a Store <a name="2-criando-a-store"></a></h4> <p>A Store é o centro de tudo, a partir dela teremos nossos Slices e usaremos em toda a aplicação, é o coração do gerenciamento de estado.</p> <p>A partir da documentação oficial do React-Redux, temos o seguinte exemplo de código:</p> <p><code class="language-plaintext highlighter-rouge">src/store/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">configureStore</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">@reduxjs/toolkit</span><span class="dl">'</span>
<span class="c1">// ...</span>

<span class="kd">const</span> <span class="nx">store</span> <span class="o">=</span> <span class="nf">configureStore</span><span class="p">({</span>
  <span class="na">reducer</span><span class="p">:</span> <span class="p">{</span>
    <span class="na">posts</span><span class="p">:</span> <span class="nx">postsReducer</span><span class="p">,</span>
    <span class="na">comments</span><span class="p">:</span> <span class="nx">commentsReducer</span><span class="p">,</span>
    <span class="na">users</span><span class="p">:</span> <span class="nx">usersReducer</span><span class="p">,</span>
  <span class="p">},</span>
<span class="p">})</span>

<span class="c1">// Infer the RootState and AppDispatch types from the store itself</span>
<span class="k">export</span> <span class="kd">type</span> <span class="nx">RootState</span> <span class="o">=</span> <span class="nb">ReturnType</span><span class="o">&lt;</span><span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">getState</span><span class="o">&gt;</span>
<span class="c1">// Inferred type: {posts: PostsState, comments: CommentsState, users: UsersState}</span>
<span class="k">export</span> <span class="kd">type</span> <span class="nx">AppDispatch</span> <span class="o">=</span> <span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">dispatch</span>
</code></pre></div></div> <p>Como ainda não temos nenhum Slice, vamos criar assim por enquanto: <code class="language-plaintext highlighter-rouge">src/store/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">configureStore</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">@reduxjs/toolkit</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">store</span> <span class="o">=</span> <span class="nf">configureStore</span><span class="p">({</span>
  <span class="na">reducer</span><span class="p">:</span> <span class="p">{},</span>
<span class="p">})</span>


<span class="k">export</span> <span class="kd">type</span> <span class="nx">RootState</span> <span class="o">=</span> <span class="nb">ReturnType</span><span class="o">&lt;</span><span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">getState</span><span class="o">&gt;</span>

<span class="k">export</span> <span class="kd">type</span> <span class="nx">AppDispatch</span> <span class="o">=</span> <span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">dispatch</span>
</code></pre></div></div> <p>Com a Store criada, agora vamos para o próximo passo: configurar o Provider, assim toda aplicação poderá utilizar o Redux.</p> <h4 id="3-configurando-o-provider-">3 Configurando o Provider <a name="3-configurando-o-provider"></a></h4> <p>Este passo é o mais simples, nunca realmente vai mudar, apenas não esqueça pois é crucial.</p> <p>Seguindo a documentação oficial, envolva o seu App com o Provider do React-Redux e passe para ele a sua Store recém criada. <code class="language-plaintext highlighter-rouge">src/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">...</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">Provider</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-redux</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">store</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./store</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">App</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./App</span><span class="dl">'</span>

<span class="kd">const</span> <span class="nx">root</span> <span class="o">=</span> <span class="nx">ReactDOM</span><span class="p">.</span><span class="nf">createRoot</span><span class="p">(</span><span class="nb">document</span><span class="p">.</span><span class="nf">getElementById</span><span class="p">(</span><span class="dl">'</span><span class="s1">root</span><span class="dl">'</span><span class="p">))</span>

<span class="nx">root</span><span class="p">.</span><span class="nf">render</span><span class="p">(</span>
  <span class="o">&lt;</span><span class="nx">Provider</span> <span class="nx">store</span><span class="o">=</span><span class="p">{</span><span class="nx">store</span><span class="p">}</span><span class="o">&gt;</span>
    <span class="o">&lt;</span><span class="nx">App</span> <span class="o">/&gt;</span>
  <span class="o">&lt;</span><span class="sr">/Provider</span><span class="err">&gt;
</span><span class="p">)</span>
</code></pre></div></div> <p>Agora chegou a hora de criarmos nossos Slices, através deles teremos nossos estados e reducers que vão fazer a mágica acontecer.</p> <h4 id="4-slices-">4 Slices <a name="4-slices"></a></h4> <p>Dentro da pasta <code class="language-plaintext highlighter-rouge">store</code> vamos criar nosso primeiro e mais simples Slice: <code class="language-plaintext highlighter-rouge">favoritesSlice.ts</code></p> <p>Antes de mais nada, gostaria de demonstrar a anatomia geral de um Slice, o que deve se repetir para todos os próximos: <code class="language-plaintext highlighter-rouge">src/store/favoritesSlice.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">createSlice</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">@reduxjs/toolkit</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">RootState</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">.</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">Movie</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../interfaces</span><span class="dl">'</span><span class="p">;</span>  

<span class="c1">// declarei aqui o estado inicial para que assim pudesse inferir um tipo</span>
<span class="c1">// desta forma o slice irá aceitar apenas filmes em seu estado </span>
<span class="kd">const</span> <span class="nx">initialState</span><span class="p">:</span> <span class="nx">Movie</span><span class="p">[]</span> <span class="o">=</span> <span class="p">[];</span>  

<span class="k">export</span> <span class="kd">const</span> <span class="nx">favorites</span> <span class="o">=</span> <span class="nf">createSlice</span><span class="p">({</span>
	<span class="c1">// nome identificador do slice</span>
	<span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">favorites</span><span class="dl">'</span><span class="p">,</span>
	<span class="c1">// estado inicial</span>
	<span class="nx">initialState</span><span class="p">,</span>
	<span class="na">reducers</span><span class="p">:</span> <span class="p">{</span>
		<span class="c1">// funções reducers</span>
	<span class="p">},</span>
<span class="p">});</span>

<span class="k">export</span> <span class="kd">const</span> <span class="p">{</span> 
	<span class="c1">// reducers</span>
<span class="p">}</span> <span class="o">=</span> <span class="nx">favorites</span><span class="p">.</span><span class="nx">actions</span><span class="p">;</span>

<span class="c1">// seletores (é uma forma de buscar dados dentro dos componentes react usando estas funções que podemos criar)</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">getFavorites</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="nx">state</span><span class="p">:</span> <span class="nx">RootState</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">state</span><span class="p">.</span><span class="nx">favorites</span><span class="p">;</span>  
<span class="c1">// RootState vai dar erro pois este slice ainda não está registrado na store, vamos ver a seguir...</span>

<span class="k">export</span> <span class="k">default</span> <span class="nx">favorites</span><span class="p">.</span><span class="nx">reducer</span><span class="p">;</span>
</code></pre></div></div> <p>Como o Slice é apenas uma fatia do estado global, precisamos que ele se integre com a nossa Store criada anteriormente, e para isso faremos o seguinte:</p> <p><code class="language-plaintext highlighter-rouge">src/store/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">configureStore</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">@reduxjs/toolkit</span><span class="dl">'</span>
<span class="k">import</span> <span class="nx">favorites</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./favoritesSlice</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">const</span> <span class="nx">store</span> <span class="o">=</span> <span class="nf">configureStore</span><span class="p">({</span>
  <span class="na">reducer</span><span class="p">:</span> <span class="p">{</span>
	  <span class="nx">favorites</span> <span class="c1">// registrar o novo slice na Store</span>
  <span class="p">},</span>
<span class="p">})</span>

<span class="c1">// agora o RootState conhece o seu novo slice e deve corrigir o erro anterior</span>
<span class="k">export</span> <span class="kd">type</span> <span class="nx">RootState</span> <span class="o">=</span> <span class="nb">ReturnType</span><span class="o">&lt;</span><span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">getState</span><span class="o">&gt;</span>

<span class="k">export</span> <span class="kd">type</span> <span class="nx">AppDispatch</span> <span class="o">=</span> <span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">dispatch</span>
</code></pre></div></div> <p>Agora que vimos como é a estrutura geral de uma Store, vamos à criação dos seus Reducers!</p> <h4 id="5-reducers-">5 Reducers <a name="5-reducers"></a></h4> <p>Vamos criar o reducer de favoritar, e como descrito anteriormente, ele deve funcionar como um botão de liga/desliga, então chamaremos de <code class="language-plaintext highlighter-rouge">toggleFavorite</code>: <code class="language-plaintext highlighter-rouge">src/store/favoritesSlice.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">favorites</span> <span class="o">=</span> <span class="nf">createSlice</span><span class="p">({</span>
<span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">favorites</span><span class="dl">'</span><span class="p">,</span>
<span class="nx">initialState</span><span class="p">,</span>
<span class="na">reducers</span><span class="p">:</span> <span class="p">{</span>
	<span class="c1">// state: o estado de favoritos</span>
	<span class="c1">// action: a ação que está sendo realizada no slice, que por sua vez contém dados na em action.payload</span>
	<span class="na">toggleFavorite</span><span class="p">:</span> <span class="p">(</span><span class="nx">state</span><span class="p">,</span> <span class="na">action</span><span class="p">:</span> <span class="p">{</span> <span class="na">payload</span><span class="p">:</span> <span class="nx">Movie</span> <span class="p">})</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="c1">// a payload é onde podemos passar dados para o nosso reducer</span>
	<span class="c1">// neste caso fiz obrigatório o envio de um dado do tipo Movie na payload</span>
		<span class="kd">const</span> <span class="nx">index</span> <span class="o">=</span> <span class="nx">state</span><span class="p">.</span><span class="nf">findIndex</span><span class="p">((</span><span class="nx">movie</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">movie</span><span class="p">.</span><span class="nx">id</span> <span class="o">===</span> <span class="nx">action</span><span class="p">.</span><span class="nx">payload</span><span class="p">.</span><span class="nx">id</span><span class="p">);</span>  
		
		<span class="k">if </span><span class="p">(</span><span class="nx">index</span> <span class="o">===</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
				<span class="nx">state</span><span class="p">.</span><span class="nf">push</span><span class="p">(</span><span class="nx">action</span><span class="p">.</span><span class="nx">payload</span><span class="p">);</span>
			<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
				<span class="nx">state</span><span class="p">.</span><span class="nf">splice</span><span class="p">(</span><span class="nx">index</span><span class="p">,</span> <span class="mi">1</span><span class="p">);</span>
			<span class="p">}</span>
		<span class="p">},</span>
	<span class="p">},</span>
<span class="p">});</span>
</code></pre></div></div> <p>Desta forma nosso primeiro reducer ganha vida, mas ainda não podemos usar ainda, temos que exportá-lo nas nossas actions: <code class="language-plaintext highlighter-rouge">src/store/favoritesSlice.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="p">{</span> <span class="nx">toggleFavorite</span> <span class="p">}</span> <span class="o">=</span> <span class="nx">favorites</span><span class="p">.</span><span class="nx">actions</span><span class="p">;</span>
</code></pre></div></div> <p>E agora sim, nosso slice de favoritos está pronto para ser utilizado!</p> <h3 id="ligado-os-dados-aos-componentes-">Ligado os dados aos componentes! <a name="ligado-os-dados-aos-componentes"></a></h3> <p>A página de favoritos é super simples, apenas iremos listar todos eles: <code class="language-plaintext highlighter-rouge">src/pages/favorites/index.tsx</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">useSelector</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-redux</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">Layout</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../../components/Layout</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">MovieCard</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../../components/MovieCard</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">getFavorites</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../../store/favoritesSlice</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">const</span> <span class="nx">Favorites</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>

	<span class="c1">// a função useSelector do react-redux é responsável por interpretar a função getFavorites e retornar os dados</span>
	<span class="kd">const</span> <span class="nx">favorites</span> <span class="o">=</span> <span class="nf">useSelector</span><span class="p">(</span><span class="nf">getFavorites</span><span class="p">());</span>
	
	<span class="k">return </span><span class="p">(</span>
		<span class="o">&lt;</span><span class="nx">Layout</span><span class="o">&gt;</span>
			<span class="o">&lt;</span><span class="nx">h1</span><span class="o">&gt;</span><span class="nx">Favorites</span><span class="o">&lt;</span><span class="sr">/h1</span><span class="err">&gt;
</span>			<span class="o">&lt;</span><span class="nx">div</span><span class="o">&gt;</span>	
				<span class="p">{</span><span class="nx">favorites</span><span class="p">.</span><span class="nx">length</span> <span class="o">===</span> <span class="mi">0</span> <span class="p">?</span> <span class="p">(</span>
					<span class="o">&lt;</span><span class="nx">p</span><span class="o">&gt;</span><span class="nx">You</span> <span class="nx">don</span><span class="dl">'</span><span class="s1">t have any favorite movies&lt;/p&gt;
				) : (
					favorites.map((movie) =&gt; &lt;MovieCard key={movie.id} movie={movie} /&gt;)
				)}
			&lt;/div&gt;
		&lt;/Layout&gt;
	);
};

export default Favorites;
</span></code></pre></div></div> <p>Logo vamos seguir para onde acontece toda ação: no componente <code class="language-plaintext highlighter-rouge">MovieCard.tsx</code></p> <p><code class="language-plaintext highlighter-rouge">src/components/MovieCard.tsx</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">FiHeart</span><span class="p">,</span> <span class="nx">FiShoppingCart</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-icons/fi</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">useDispatch</span><span class="p">,</span> <span class="nx">useSelector</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-redux</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">useNavigate</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-router-dom</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">Movie</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../interfaces</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">getFavorites</span><span class="p">,</span> <span class="nx">toggleFavorite</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../store/favoritesSlice</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">type</span> <span class="nx">MovieCardProps</span> <span class="o">=</span> <span class="p">{</span>
	<span class="na">movie</span><span class="p">:</span> <span class="nx">Movie</span><span class="p">;</span>
<span class="p">};</span>  

<span class="kd">const</span> <span class="nx">MovieCard</span> <span class="o">=</span> <span class="p">({</span> <span class="nx">movie</span> <span class="p">}:</span> <span class="nx">MovieCardProps</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="kd">const</span> <span class="nx">navigate</span> <span class="o">=</span> <span class="nf">useNavigate</span><span class="p">();</span>

	<span class="c1">// aqui utilizamos o useDispatch para que o Dispatcher do Redux possa executar os reducers que passarmos</span>
	<span class="kd">const</span> <span class="nx">dispatch</span> <span class="o">=</span> <span class="nf">useDispatch</span><span class="p">();</span>

	<span class="c1">// novamente buscamos todos os favoritos com o seletor</span>
	<span class="kd">const</span> <span class="nx">favorites</span> <span class="o">=</span> <span class="nf">useSelector</span><span class="p">(</span><span class="nf">getFavorites</span><span class="p">());</span>

	<span class="kd">const</span> <span class="nx">isFavorite</span> <span class="o">=</span> <span class="o">!!</span><span class="nx">favorites</span><span class="p">.</span><span class="nf">find</span><span class="p">((</span><span class="nx">fav</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">fav</span><span class="p">.</span><span class="nx">id</span> <span class="o">===</span> <span class="nx">movie</span><span class="p">.</span><span class="nx">id</span><span class="p">);</span>  

	<span class="kd">const</span> <span class="nx">handleAddToFavorites</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
		<span class="c1">// aqui despachamos o reducer toggleFavorite passando os dados do filme em questão</span>
		<span class="nf">dispatch</span><span class="p">(</span><span class="nf">toggleFavorite</span><span class="p">(</span><span class="nx">movie</span><span class="p">));</span>
	<span class="p">};</span>  

	<span class="kd">const</span> <span class="nx">price</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Intl</span><span class="p">.</span><span class="nc">NumberFormat</span><span class="p">(</span><span class="dl">'</span><span class="s1">en-US</span><span class="dl">'</span><span class="p">,</span> <span class="p">{</span>
		<span class="na">style</span><span class="p">:</span> <span class="dl">'</span><span class="s1">currency</span><span class="dl">'</span><span class="p">,</span>
		<span class="na">currency</span><span class="p">:</span> <span class="dl">'</span><span class="s1">USD</span><span class="dl">'</span><span class="p">,</span>
	<span class="p">}).</span><span class="nf">format</span><span class="p">(</span><span class="nx">movie</span><span class="p">.</span><span class="nx">price</span><span class="p">);</span>

	<span class="k">return </span><span class="p">(</span>
	
	<span class="o">&lt;</span><span class="nx">div</span><span class="o">&gt;</span>	
		<span class="o">&lt;</span><span class="nx">h2</span><span class="o">&gt;</span>	
			<span class="p">{</span><span class="nx">movie</span><span class="p">.</span><span class="nx">title</span><span class="p">}</span>
		<span class="o">&lt;</span><span class="sr">/h2</span><span class="err">&gt;
</span>		<span class="o">&lt;</span><span class="nx">img</span> <span class="nx">src</span><span class="o">=</span><span class="p">{</span><span class="nx">movie</span><span class="p">.</span><span class="nx">cover</span><span class="p">}</span> <span class="nx">alt</span><span class="o">=</span><span class="p">{</span><span class="nx">movie</span><span class="p">.</span><span class="nx">title</span><span class="p">}</span><span class="sr">/</span><span class="err">&gt;
</span>		
		<span class="o">&lt;</span><span class="nx">div</span><span class="o">&gt;</span>	
			<span class="o">&lt;</span><span class="nx">span</span> <span class="nx">className</span><span class="o">=</span><span class="dl">'</span><span class="s1">text-lg font-bold mr-auto</span><span class="dl">'</span><span class="o">&gt;</span><span class="na">Price</span><span class="p">:</span> <span class="p">{</span><span class="nx">price</span><span class="p">}</span><span class="o">&lt;</span><span class="sr">/span</span><span class="err">&gt;
</span>			
			<span class="o">&lt;</span><span class="nx">button</span><span class="o">&gt;</span>	
				<span class="o">&lt;</span><span class="nx">FiShoppingCart</span> <span class="nx">size</span><span class="o">=</span><span class="p">{</span><span class="mi">24</span><span class="p">}</span> <span class="sr">/</span><span class="err">&gt;
</span>			<span class="o">&lt;</span><span class="sr">/button</span><span class="err">&gt;
</span>			
			<span class="o">&lt;</span><span class="nx">button</span>
			<span class="nx">onClick</span><span class="o">=</span><span class="p">{</span><span class="nx">handleAddToFavorites</span><span class="p">}</span>
			<span class="nx">className</span><span class="o">=</span><span class="p">{</span><span class="nx">isFavorite</span> <span class="o">&amp;&amp;</span> <span class="dl">'</span><span class="s1">bg-red-500</span><span class="dl">'</span><span class="p">}</span><span class="o">&gt;</span>	
				<span class="o">&lt;</span><span class="nx">FiHeart</span>
					<span class="nx">size</span><span class="o">=</span><span class="p">{</span><span class="mi">24</span><span class="p">}</span>
					<span class="nx">className</span><span class="o">=</span><span class="p">{</span><span class="nx">isFavorite</span> <span class="o">&amp;&amp;</span> <span class="dl">'</span><span class="s1">text-red-500</span><span class="dl">'</span><span class="p">}</span>
				<span class="sr">/</span><span class="err">&gt;
</span>			<span class="o">&lt;</span><span class="sr">/button</span><span class="err">&gt;
</span>		<span class="o">&lt;</span><span class="sr">/div</span><span class="err">&gt;
</span>		
		<span class="o">&lt;</span><span class="nx">button</span>
		<span class="nx">onClick</span><span class="o">=</span><span class="p">{()</span> <span class="o">=&gt;</span> <span class="nf">navigate</span><span class="p">(</span><span class="s2">`/movie/</span><span class="p">${</span><span class="nx">movie</span><span class="p">.</span><span class="nx">id</span><span class="p">}</span><span class="s2">`</span><span class="p">)}</span><span class="o">&gt;</span>	
		<span class="nx">More</span>
		<span class="o">&lt;</span><span class="sr">/button</span><span class="err">&gt;
</span>		
	<span class="o">&lt;</span><span class="sr">/div</span><span class="err">&gt;
</span>	<span class="p">);</span>
<span class="p">};</span>

<span class="k">export</span> <span class="k">default</span> <span class="nx">MovieCard</span><span class="p">;</span>
</code></pre></div></div> <p>Agora o componente <code class="language-plaintext highlighter-rouge">MovieCard</code> é capaz e favoritar os filmes e nossa página de favoritos irá exibi-los, aparentemente está tudo funcionando, mas existe um pequeno problema: <em>Estados React são reiniciados quando a tela atualiza</em>.</p> <h2 id="persistindo-dados-">Persistindo dados <a name="persistindo-dados"></a></h2> <p>Uma vez que os dados da nossa Store naturalmente serão jogados fora ao recarregar a página, precisamos que estes dados sejam armazenados localmente no navegador do usuário.</p> <p>Desta forma mesmo que feche o navegador ou desligue o computador, quando voltar para a aplicação seus favoritos estarão no mesmo lugar onde deixou.</p> <p>Em alguns casos esta questão é trivial, nem sempre existe a necessidade de armazenar estes dados, mas nesta aqui é super importante que eu persista estes favoritos.</p> <p>Mas a solução é simples: dentro do reducer, depois que atualizar o estado, utilize o localStore ou sessionStorage para armazenar estes dados no navegador do usuário, e no initialState é só buscar os dados no navegador!</p> <p>Em teoria sim, isso funciona, mas vai dar um trabalho enorme para implementar isso na mão, fora outras questões que existem.</p> <p>Mas não tem problema, temos o <a href="https://github.com/rt2zz/redux-persist">Redux-Persist</a>, uma solução completa e simplista para persistir os dados do seu Redux.</p> <p>Vamos à implementação:</p> <p>Primeiro instale o pacote: <code class="language-plaintext highlighter-rouge">yarn add redux-persist</code> ou <code class="language-plaintext highlighter-rouge">npm i redux-persist</code></p> <p>O passo essencial agora é adaptar nossa Store para usar o Redux Persist: <code class="language-plaintext highlighter-rouge">src/store/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">combineReducers</span><span class="p">,</span> <span class="nx">configureStore</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">@reduxjs/toolkit</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">persistReducer</span><span class="p">,</span> <span class="nx">persistStore</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">redux-persist</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">storage</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">redux-persist/lib/storage</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">cart</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./cartSlice</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="nx">favorites</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./favoritesSlice</span><span class="dl">'</span><span class="p">;</span>

<span class="c1">// configuração básica do redux-persist</span>
<span class="kd">const</span> <span class="nx">persistConfig</span> <span class="o">=</span> <span class="p">{</span>
	<span class="na">key</span><span class="p">:</span> <span class="dl">'</span><span class="s1">root</span><span class="dl">'</span><span class="p">,</span>
	<span class="c1">// podemos utilizar outras formas de armazenamento, mas a padrão utiliza a localStorage do navegador</span>
	<span class="nx">storage</span><span class="p">,</span>
<span class="p">};</span>

<span class="c1">// vamos mudar a forma como recebemos os reducers</span>
<span class="c1">// agora combinamos todos os reducers em um único</span>
<span class="kd">const</span> <span class="nx">rootReducer</span> <span class="o">=</span> <span class="nf">combineReducers</span><span class="p">({</span>
	<span class="nx">favorites</span>
<span class="p">});</span>

<span class="c1">// o persistedReducer vai comportar dos nossos reducers e com as configurações apropriadas que definimos</span>
<span class="kd">const</span> <span class="nx">persistedReducer</span> <span class="o">=</span> <span class="nf">persistReducer</span><span class="p">(</span><span class="nx">persistConfig</span><span class="p">,</span> <span class="nx">rootReducer</span><span class="p">);</span>

<span class="c1">// a store é finalmente criada com nosso reducer especial</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">store</span> <span class="o">=</span> <span class="nf">configureStore</span><span class="p">({</span>
	<span class="na">reducer</span><span class="p">:</span> <span class="nx">persistedReducer</span><span class="p">,</span>
<span class="p">});</span>

<span class="c1">// responsável por persistir os dados da store</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">persistor</span> <span class="o">=</span> <span class="nf">persistStore</span><span class="p">(</span><span class="nx">store</span><span class="p">);</span>

<span class="k">export</span> <span class="kd">type</span> <span class="nx">RootState</span> <span class="o">=</span> <span class="nb">ReturnType</span><span class="o">&lt;</span><span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">getState</span><span class="o">&gt;</span><span class="p">;</span>

<span class="k">export</span> <span class="kd">type</span> <span class="nx">AppDispatch</span> <span class="o">=</span> <span class="k">typeof</span> <span class="nx">store</span><span class="p">.</span><span class="nx">dispatch</span><span class="p">;</span>
</code></pre></div></div> <p>E por último, vamos configurar o provider do Redux-Persist: <code class="language-plaintext highlighter-rouge">src/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">Provider</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react-redux</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">PersistGate</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">redux-persist/integration/react</span><span class="dl">'</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">persistor</span><span class="p">,</span> <span class="nx">store</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">../store</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">const</span> <span class="nx">root</span> <span class="o">=</span> <span class="nx">ReactDOM</span><span class="p">.</span><span class="nf">createRoot</span><span class="p">(</span><span class="nb">document</span><span class="p">.</span><span class="nf">getElementById</span><span class="p">(</span><span class="dl">'</span><span class="s1">root</span><span class="dl">'</span><span class="p">))</span>

<span class="nx">root</span><span class="p">.</span><span class="nf">render</span><span class="p">(</span>
<span class="c1">// o mesmo provider que utilizamos antes</span>
  <span class="o">&lt;</span><span class="nx">Provider</span> <span class="nx">store</span><span class="o">=</span><span class="p">{</span><span class="nx">store</span><span class="p">}</span><span class="o">&gt;</span>
  	<span class="c1">// agora temos que passar o PersistGate em volta da aplicação</span>
	<span class="o">&lt;</span><span class="nx">PersistGate</span> <span class="nx">loading</span><span class="o">=</span><span class="p">{</span><span class="kc">null</span><span class="p">}</span> <span class="nx">persistor</span><span class="o">=</span><span class="p">{</span><span class="nx">persistor</span><span class="p">}</span><span class="o">&gt;</span>
		<span class="o">&lt;</span><span class="nx">App</span> <span class="o">/&gt;</span>
	<span class="o">&lt;</span><span class="sr">/PersistGate</span><span class="err">&gt;
</span>  <span class="o">&lt;</span><span class="sr">/Provider</span><span class="err">&gt;
</span><span class="p">)</span>
</code></pre></div></div> <p>Simples assim, agora os dados irão naturalmente persistir no localStorage.</p> <p>Ainda sim vale a pena que você se familiarize com temas comuns do Redux-Persist como:</p> <ul> <li>Blacklist: uma lista dos slices que não devem ser permitidos persistir</li> <li>Whitelist: uma lista dos únicos slices que devem persistir</li> <li>stateReconciler: Os reconciliadores de estado definem como o estado de entrada é mesclado com o estado inicial</li> </ul> <p>Todos estes temas você encontra na <a href="https://github.com/rt2zz/redux-persist">documentação oficial do Redux-Persist</a>.</p> <h3 id="seletores-personalizados-">Seletores personalizados <a name="seletores-personalizados"></a></h3> <p>Este ponto é bem simples mas ainda sim gostaria de trazer atenção para ele pois seu bom uso é extremamente benéfico no React.</p> <p>Esta pequena função a seguir faz parte do exemplo dado acima na criação do slice.</p> <p><code class="language-plaintext highlighter-rouge">app/store/favoritesSlice.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">getFavorites</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="nx">state</span><span class="p">:</span> <span class="nx">RootState</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">state</span><span class="p">.</span><span class="nx">favorites</span><span class="p">;</span>
</code></pre></div></div> <p>Neste exemplo apenas retornamos todos os favoritos disponíveis no slice, mas podemos realizar diversas modificação de acordo com a necessidade, como por exemplo:</p> <p><code class="language-plaintext highlighter-rouge">app/store/favoritesSlice.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// passando algum parâmetro</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">getSortedFavorites</span> <span class="o">=</span> <span class="p">(</span><span class="nx">sorting</span><span class="p">:</span><span class="dl">'</span><span class="s1">asc</span><span class="dl">'</span><span class="o">|</span><span class="dl">'</span><span class="s1">desc</span><span class="dl">'</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="nx">state</span><span class="p">:</span> <span class="nx">RootState</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">state</span><span class="p">.</span><span class="nx">favorites</span><span class="p">.</span><span class="nf">sort</span><span class="p">(...</span><span class="cm">/* função para ordenar */</span><span class="p">);</span>
																		<span class="c1">// Editando o retorno													</span>
<span class="k">export</span> <span class="kd">const</span> <span class="nx">getFormattedFavorites</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="nx">state</span><span class="p">:</span> <span class="nx">RootState</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">state</span><span class="p">.</span><span class="nx">favorites</span><span class="p">.</span><span class="nf">map</span><span class="p">(...</span><span class="cm">/* função para formatar os dados */</span><span class="p">);</span>
</code></pre></div></div> <h2 id="redux-com-nextjs-">Redux com NextJS <a name="redux-com-nextjs"></a></h2> <p>Existe um pequeno problema ao utilizar Redux com NextJS:</p> <p>NextJS é executado no lado do cliente e no lado do servidor, e no lado do servidor <em>NÃO EXISTE WEB STORAGE</em>!</p> <p>Contudo ainda tem solução:</p> <ul> <li>Quando a aplicação for executada no lado do cliente utilizaremos o storage padrão;</li> <li>Quando a aplicação for executada no lado do servidor utilizaremos o storage fake.</li> </ul> <p>Nossas funções do Redux estão sendo utilizadas apenas no lado do cliente, mas mesmo assim, encontraremos erros em tempo de execução devido a natureza de Server Side Rendering do NextJS.</p> <p>Logo, vamos criar nosso novo storage:</p> <p><code class="language-plaintext highlighter-rouge">src/store/storage.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nx">createWebStorage</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">redux-persist/lib/storage/createWebStorage</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">const</span> <span class="nx">createNoopStorage</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="k">return</span> <span class="p">{</span>
		<span class="nf">getItem</span><span class="p">(</span><span class="na">_key</span><span class="p">:</span> <span class="kr">any</span><span class="p">)</span> <span class="p">{</span>
			<span class="k">return</span> <span class="nb">Promise</span><span class="p">.</span><span class="nf">resolve</span><span class="p">(</span><span class="kc">null</span><span class="p">);</span>
		<span class="p">},</span>
		<span class="nf">setItem</span><span class="p">(</span><span class="na">_key</span><span class="p">:</span> <span class="kr">any</span><span class="p">,</span> <span class="na">value</span><span class="p">:</span> <span class="kr">any</span><span class="p">)</span> <span class="p">{</span>
			<span class="k">return</span> <span class="nb">Promise</span><span class="p">.</span><span class="nf">resolve</span><span class="p">(</span><span class="nx">value</span><span class="p">);</span>
		<span class="p">},</span>
		<span class="nf">removeItem</span><span class="p">(</span><span class="na">_key</span><span class="p">:</span> <span class="kr">any</span><span class="p">)</span> <span class="p">{</span>
			<span class="k">return</span> <span class="nb">Promise</span><span class="p">.</span><span class="nf">resolve</span><span class="p">();</span>
		<span class="p">},</span>
	<span class="p">};</span>
<span class="p">};</span>

<span class="kd">const</span> <span class="nx">storage</span> <span class="o">=</span>
	<span class="k">typeof</span> <span class="nb">window</span> <span class="o">!==</span> <span class="dl">'</span><span class="s1">undefined</span><span class="dl">'</span>
		<span class="p">?</span> <span class="nf">createWebStorage</span><span class="p">(</span><span class="dl">'</span><span class="s1">local</span><span class="dl">'</span><span class="p">)</span>
		<span class="p">:</span> <span class="nf">createNoopStorage</span><span class="p">();</span>

<span class="k">export</span> <span class="k">default</span> <span class="nx">storage</span><span class="p">;</span>
</code></pre></div></div> <p>E assim atualizamos a nossa Store: <code class="language-plaintext highlighter-rouge">src/store/index.ts</code></p> <div class="language-typescript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">...</span>
<span class="c1">// importação atualizada</span>
<span class="k">import</span> <span class="nx">storage</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">./storage</span><span class="dl">'</span><span class="p">;</span>

<span class="kd">const</span> <span class="nx">persistConfig</span> <span class="o">=</span> <span class="p">{</span>
	<span class="na">key</span><span class="p">:</span> <span class="dl">'</span><span class="s1">root</span><span class="dl">'</span><span class="p">,</span>
	<span class="nx">storage</span><span class="p">,</span>
<span class="p">};</span>
</code></pre></div></div> <p>Gerenciamento avançado de estados é um tema crucial quando se fala de desenvolvimento web, compreender e utilizar a ferramenta Redux e outros semelhantes me ajudou bastante para desenvolver softwares mais sofisticados e complexos.</p> <p>Espero que tenham gostado desta super introdução ao tema e à ferramenta. Se quiser deixar alguma nota, complementar em algo ou se houver alguma ideia interessante, não deixe de compartilhar comigo.</p> <h2 id="referências-">Referências <a name="referencias"></a></h2> <p>Dias, D. L. (2022, 20 de fevereiro). Flux: A arquitetura JavaScript que funciona. Medium. <a href="https://dayvsonlima.medium.com/flux-a-arquitetura-javascript-que-funciona-1197857464b8">https://dayvsonlima.medium.com/flux-a-arquitetura-javascript-que-funciona-1197857464b8</a></p> <p>FreeCodeCamp.org. (2022, 2 de agosto). Evite prop drilling em React. FreeCodeCamp.org. <a href="https://www.freecodecamp.org/news/avoid-prop-drilling-in-react/">https://www.freecodecamp.org/news/avoid-prop-drilling-in-react/</a></p> <p>Redux.js.org. (s.d.). Redux. Redux.js.org. <a href="https://redux.js.org/">https://redux.js.org/</a></p> <p>React-redux.js.org. (s.d.). React-redux. React-redux.js.org. <a href="https://react-redux.js.org/">https://react-redux.js.org/</a></p> <p>FreeCodeCamp.org. (2022, 12 de março). O que é Redux store, ações e redutores explicados. FreeCodeCamp.org. <a href="https://www.freecodecamp.org/news/what-is-redux-store-actions-reducers-explained/">https://www.freecodecamp.org/news/what-is-redux-store-actions-reducers-explained/</a></p> <p>GitHub. (s.d.). redux-persist. GitHub. <a href="https://github.com/rt2zz/redux-persist">https://github.com/rt2zz/redux-persist</a></p>]]></content><author><name></name></author><category term="frontend"/><category term="frontend"/><category term="reactjs"/><category term="typescript"/><category term="javascript"/><category term="redux"/><category term="state-management"/><summary type="html"><![CDATA[Aprenda de forma simples e prática como usar Redux para gerenciar estados complexos em aplicações ReactJS.]]></summary></entry><entry><title type="html">SOLID no Frontend</title><link href="https://robertheory.github.io/blog/2023/solid-no-frontend/" rel="alternate" type="text/html" title="SOLID no Frontend"/><published>2023-09-23T16:40:16+00:00</published><updated>2023-09-23T16:40:16+00:00</updated><id>https://robertheory.github.io/blog/2023/solid-no-frontend</id><content type="html" xml:base="https://robertheory.github.io/blog/2023/solid-no-frontend/"><![CDATA[<p>Começar um novo projeto é o caminho fácil, o desafio vem em manter este projeto saudável ao longo do tempo.</p> <p>Aqui trarei exemplos de conceito e código, demonstrando como aplicar os tão famosos princípios SOLID no frontend para uma aplicação sustentável e resistente ao tempo.</p> <h2 id="table-of-contents">Table Of Contents</h2> <ul> <li><a href="#chapter-1">O Problema do ReactJS…</a></li> <li><a href="#chapter-2">Resolvendo alguns problemas</a></li> <li><a href="#chapter-3">Aplicando SOLID no ReactJS</a> <ul> <li><a href="#chapter-3.1">Single Responsability Principle</a></li> <li><a href="#chapter-3.2">Open-Closed Principle</a></li> <li><a href="#chapter-3.3">Liskov Substituition Principle</a></li> <li><a href="#chapter-3.4">Interface Segregation Principle</a></li> <li><a href="#chapter-3.5">Dependency Inversion Principle</a></li> </ul> </li> </ul> <h2 id="o-problema-do-reactjs-">O Problema do ReactJS… <a name="chapter-1"></a></h2> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/04b5clsdy52u1wugqf5h.png" alt="React projects begin easy and angular begin hard, after 1 year the situation shifts"/></p> <p>Na verdade o que vou falar aqui não em si um problema, mas uma característica que pode ser problemática:</p> <h3 id="flexibilidade">Flexibilidade</h3> <p>A flexibilidade do React é algo notável, uma vez que essa biblioteca permite realizar escolhas para cada peça do quebra-cabeça que é o nosso frontend.</p> <p>Essa flexibilidade é um ponto chave para o React, ela te permite realizar os mais diversos casos de uso e usar a sua criatividade de forma abundante no código.</p> <p>No entanto isso não é apenas um ponto positivo…</p> <p>Frequentemente aplicações ReactJS chegam em um ponto onde a manutenção é dificultosa, repetindo-se muito código e tendo bastante retrabalho, e se você ainda não viu isso ocorrendo, temos 3 possibilidades para considerar:</p> <ul> <li>Seu projeto é muito novo;</li> <li>Seu projeto é pequeno;</li> <li>Existe alguém cuidando da arquitetura e design de código do seu frontend.</li> </ul> <p>Quando falamos de design de código e arquitetura de software estamos indo muito além de estrutura de pastas.</p> <p>No frontend, mais especificamente no ReactJS, esses conceitos têm um lugar especial, uma vez que essa biblioteca não oferece uma estrutura opinada pensada para escalar e ser de fácil manutenção, ela deixa tudo para você decidir, o que para muitas pessoas não é realmente recomendado.</p> <h2 id="resolvendo-alguns-problemas-">Resolvendo alguns problemas <a name="chapter-2"></a></h2> <p>A componentização é uma mão na roda na hora de programar interfaces, mas criar componentes cegamente não ajuda em nada.</p> <p>Uma coisa que você já deve ter percebido é que a forma como seus componentes se relacionam está diretamente ligada a qualidade do seu frontend.</p> <p>Algumas más práticas que existem:</p> <ul> <li>Prop Drilling</li> </ul> <p>Quando um componente pai passa um estado para o filho, que passa para seu filho, que passa para seu filho, que passa para seu filho, etc.</p> <ul> <li>Context APIs desnecessárias</li> </ul> <p>Já criou um contexto e depois percebeu que está sendo sub utilizado, ou que não faz sentido usar na aplicação inteira? A melhor forma aqui é ver aplicações maiores e sua utilização de seus próprios contextos, pode ser esclarecedor.</p> <ul> <li>Pacotes desnecessários</li> </ul> <p>Calma ai, quer dizer que você precisa baixar um pacote para validar um CPF que poderia utilizar uma RegExp? Tem alguma vantagem em usar o Axios ao invés da Fetch API? Pergunte-se, entenda o uso de cada coisa e saiba suas alternativas.</p> <ul> <li>Hooks usados de forma errada ou sub utilizados</li> </ul> <p>Por vezes um hook mal implementado pode fazer com que algumas requisições seja feitas a mais ou que algum regra da sua aplicação não seja devidamente validada, obviamente podemos chegar a problemas de segurança e de desempenho aqui.</p> <p>Requisitos para mandar bem no design de código no ReactJS:</p> <ol> <li>Dominar a base: JavaScript ou TypeScript;</li> <li>React Hooks;</li> <li>Context API;</li> <li>Conheça o terreno: saiba as opções de pacotes que tem para fazer cada função.</li> </ol> <h3 id="1-dominar-a-base">1. Dominar a base</h3> <p>Essa não é difícil de explicar: seja JS ou TS, se você não estiver seguro do que está fazendo e se não tiver algum conhecimento prévio de estruturas de dados e do funcionamento dessas tecnologias isso torna-se um fator limitante muito grande. Conforme você verá aqui, alguns conceitos são mais abstratos que outros, e certas abstrações ficam mais simples de se entender depois de um certo tempo batendo a cabeça no código ou sentindo a necessidade natural de resolver um problema no código.</p> <h3 id="2-react-hooks">2. React Hooks</h3> <p>Sim, todo dia lança um React Hook novo e você vai ter mesmo que ficar se atualizando nisso??? Infelizemente você está na área de tecnologia, se atulizar é algo intrínseco da sua carreira. Mas olhe por esse lado: Os Hooks são seus aliados, eles resolvem problemas muito importantes do ciclo de vida do React, se existe um novo Hook provavelmente ele vai resolver um problema interessante e pode te ajudar em algo, então pelo menos saiba o que eles fazem, isso já é de grande ajuda.</p> <h3 id="3-context-api">3. Context API</h3> <p>A amada Context API é uma ferramenta sensacional que temos no ReactJS para criar nossos próprios contextos e disponibilizá-los por toda aplicação ou onde acharmos necessário. Qual a importância dela? Criando nossos próprios contextos podemos atingir várias metas:</p> <ul> <li>Diminuir repetição de código;</li> <li>Concentrar o controle de uma funcionlidade em um só lugar;</li> <li>Criar abstrações de alto nível;</li> <li>Gerenciar melhor o ciclo de vida da aplicação.</li> </ul> <h3 id="4-conheça-o-terreno">4. Conheça o terreno</h3> <p>Conhecer onde está pisando te dá maior segurança para seguir em frente. Entender melhor do ecossistema do ReactJS e do JavaScript é essencial para que você encontre as peças certas para seu quebra-cabeça. Como temos muitas opções, é muito importante que você pelo menos esteja ciente dos prós e contras das principais para tomar decisões conscientes.. Algumas das decições que temos:</p> <ul> <li>CSS: CSS, CSS Modules, SASS, Styled-Components, TailwindCSS;</li> <li>Gerenciamento de estado: Redux, Recoil, nenhuma hahaha</li> <li>Router: React Router Dom, Appwrite</li> <li>Testes: Jest, Cypress, Playwright, Selenium</li> <li>Formulários: React Hook Form, Formik, Redux-form</li> <li>HTTP requests: Fetch API, Axios</li> </ul> <p>Nossa, são muitas opções mesmo! Não listei todas, apenas algumas mais conhecidas, mas deu para entender que, pela variedade, temos muitas coisas no nosso cinto de utilidades.</p> <h2 id="solid-no-reactjs-">SOLID no ReactJS <a name="chapter-3"></a></h2> <p>Agora chegou a hora, chega de enrolação, vamos falar de como aplicar os princípios SOLID para deixar seu frontend mais sênior!</p> <p>Os conceitos SOLID estão diretamente ligados à <code class="language-plaintext highlighter-rouge">Programação Orientada a Objetos</code>, logo é interessante que você seja familiar com esses conceitos para tirar melhor proveito deste conteúdo.</p> <p>Todos os exemplos de código são parte do repositório que criei para este artigo: https://github.com/robertheory/solid-react-js</p> <p>Os exemplos são inspirados em situações reais e também nas referências ao final do artigo.</p> <h3 id="s--single-responsibility-">S — Single Responsibility <a name="chapter-3.1"></a></h3> <p>Seus componentes deve ter responsabilidades bem definidas e isoladas.</p> <p>Se o seu componente faz muita coisa, pode ser uma boa oportunidade de componentizar.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/vzsl0tkevnb5yluiadcc.png" alt="Single Responsibility"/> *All illustrations in this article are by <a href="https://medium.com/@ugonnat">Ugonna Thelma</a></p> <p>Exemplo: Neste exemplo, apresento um componente consulta em API e ordenação de resultados que concetra toda a lógica da funcionalidade em si próprio:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const SearchAndSort = () =&gt; {
  const [sorting, setSorting] = useState&lt;'asc' | 'desc'&gt;('asc');

  const [search, setSearch] = useState&lt;string&gt;('');

  const [data, setData] = useState&lt;any[]&gt;([]);

  const [loading, setLoading] = useState&lt;boolean&gt;(false);

  const [error, setError] = useState&lt;string&gt;('');

  const handleSort = () =&gt; {
    setSorting(sorting === 'asc' ? 'desc' : 'asc');
  };

  const handleSearch = async () =&gt; {
    try {
      setLoading(true);
      const response = await fetch(
        `https://jsonplaceholder.typicode.com/todos?title=${search}`
      );

      const data = await response.json();

      if (!response.ok) {
        throw new Error('Something went wrong');
      }

      setData(data);
    } catch (error) {
      setError(error.message);
    } finally {
      setLoading(false);
    }
  };

  if (loading) {
    return &lt;div&gt;Loading...&lt;/div&gt;;
  }

  if (error) {
    return (
      &lt;div&gt;
        {error}

        &lt;button onClick={() =&gt; setError('')}&gt;Retry&lt;/button&gt;
      &lt;/div&gt;
    );
  }

  return (
    &lt;div&gt;
      &lt;div&gt;
        &lt;input
          type='text'
          value={search}
          onChange={(e) =&gt; setSearch(e.target.value)}
        /&gt;
        &lt;button onClick={handleSearch} disabled={loading}&gt;
          Search
        &lt;/button&gt;
      &lt;/div&gt;
      &lt;div&gt;
        &lt;button onClick={handleSort}&gt;Sort&lt;/button&gt;
      &lt;/div&gt;
      &lt;div&gt;
        {data.map((item, index) =&gt; (
          &lt;div key={index}&gt;{item.title}&lt;/div&gt;
        ))}
      &lt;/div&gt;
    &lt;/div&gt;
  );
};
</code></pre></div></div> <p>Ele é candidato a ser dividido em várias partes, podemos citar:</p> <ul> <li>Acesso à API e retorno de dados;</li> <li>Interação com a busca e feedback visual;</li> <li>Apresentação do resultado;</li> <li>Ordenação do resultado.</li> </ul> <p>Cada um destes itens é passível de se tornar um novo componente, onde podemos isolar completamente a sua lógica dentro de si para que os datalhes de implementação sejam abstraídos.</p> <p>O caso de <code class="language-plaintext highlighter-rouge">Acesso à API e retorno de dados</code> é até mais interessante pois ele nem mesmo tem a ncessidade de ser um componente, pode ser uma função separada que recebe o input de busca e retorna os resultados, dessa forma, o componente que utilizar essa função, nem mesmo precisa saber como que os dados estão sendo buscados, criando uma abstração maior ainda e um menor acoplamento a métodos de busca de dados (RestAPI, GraphQL, FileSystem, etc).</p> <p>Segue o exemplo após a refatoração:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const seachToDos = async (search: string) =&gt; {
  const response = await fetch(
    `https://jsonplaceholder.typicode.com/todos?title=${search}`
  );

  const data = (await response.json()) as Todo[];

  if (!response.ok) {
    throw new Error('Something went wrong');
  }

  return data;
};

const SearchComponent = ({
  onSearch,
}: {
  onSearch: (todos: Todo[]) =&gt; void;
}) =&gt; {
  const [search, setSearch] = useState&lt;string&gt;('');
  const [loading, setLoading] = useState&lt;boolean&gt;(false);
  const [error, setError] = useState&lt;string&gt;('');

  const handleSearch = async () =&gt; {
    try {
      setLoading(true);

      const data = await seachToDos(search);

      onSearch(data);
    } catch (error) {
      setError(error.message);
    } finally {
      setLoading(false);
    }
  };

  if (loading) {
    return &lt;div&gt;Loading...&lt;/div&gt;;
  }

  if (error) {
    return (
      &lt;div&gt;
        {error}

        &lt;button onClick={() =&gt; setError('')}&gt;Retry&lt;/button&gt;
      &lt;/div&gt;
    );
  }

  return (
    &lt;div&gt;
      &lt;input
        type='text'
        value={search}
        onChange={(e) =&gt; setSearch(e.target.value)}
      /&gt;
      &lt;button onClick={handleSearch} disabled={loading}&gt;
        Search
      &lt;/button&gt;
    &lt;/div&gt;
  );
};

const ListTodoComponent = ({ todos }: { todos: Todo[] }) =&gt; (
  &lt;div&gt;
    {todos.map((item, index) =&gt; (
      &lt;div key={index}&gt;
        &lt;h1&gt;{item.title}&lt;/h1&gt;
        &lt;p&gt;{item.completed ? 'Completed' : 'Not completed'}&lt;/p&gt;
      &lt;/div&gt;
    ))}
  &lt;/div&gt;
);

const SearchAndSort = () =&gt; {
  const [sorting, setSorting] = useState&lt;'asc' | 'desc'&gt;('asc');

  const [data, setData] = useState&lt;Todo[]&gt;([]);

  const handleSort = () =&gt; {
    setSorting(sorting === 'asc' ? 'desc' : 'asc');
  };

  return (
    &lt;div&gt;
      &lt;SearchComponent onSearch={setData} /&gt;

      &lt;button onClick={handleSort}&gt;Sort&lt;/button&gt;

      &lt;ListTodoComponent todos={data} /&gt;
    &lt;/div&gt;
  );
};
</code></pre></div></div> <h3 id="o--open-closed-">O — Open-Closed <a name="chapter-3.2"></a></h3> <p>Componentes devem ser abertos para extensão e fechados para modificação.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/1e287wroeusvowam63ah.png" alt="Open-Closed"/></p> <p>Neste exemplo, trago um simples botão com ícone que deve variar entre os tipos <code class="language-plaintext highlighter-rouge">add</code> e <code class="language-plaintext highlighter-rouge">remove</code>.</p> <p>A depender do tipo do botão, o ícone e a cor devem mudar, contudo essa implementação se torna bem problemática ao passo de que você precisa adicionar mais variantes para este botão.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type ButtonProps = {
  type: 'add' | 'remove';
  text: string;
};


const ButtonWithIcon = ({ type, text }: ButtonProps) =&gt; {
  const buttonStyles = {
    backgroundColor: type === 'add' ? 'green' : 'red',
    color: 'white',
    padding: '10px 20px',
    border: 'none',
    borderRadius: '5px',
    cursor: 'pointer',
  };

  return (
    &lt;&gt;
      &lt;button style={buttonStyles}&gt;{text}&lt;/button&gt;
      {type === 'add' &amp;&amp; &lt;i className='material-icons'&gt;add&lt;/i&gt;}
      {type === 'remove' &amp;&amp; &lt;i className='material-icons'&gt;remove&lt;/i&gt;}
    &lt;/&gt;
  );
};

</code></pre></div></div> <p>Em um cenário onde há a necessidade de se criar mais variantes para este mesmo botão, acabaria ficando inviável e extremamente verboso adicionar cada vez mais condicionais dentro dele.</p> <p>A solução é simples: vamos tornar o componente extensível!</p> <p>Tornar o componente extensível significa que os detalhes sobre a variante do botão ficarão totalmente a encargo de quem o implementar, vejamos melhor no exemplo a seguir:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type ButtonProps = {
  text: string;
  icon: string;
  color: string;
};

const ButtonWithIconExtended = ({ color, icon, text }: ButtonProps) =&gt; {
  const buttonStyles = {
    backgroundColor: color,
    color: 'white',
    padding: '10px 20px',
    border: 'none',
    borderRadius: '5px',
    cursor: 'pointer',
  };

  return (
    &lt;&gt;
      &lt;button style={buttonStyles}&gt;{text}&lt;/button&gt;
      &lt;i className='material-icons'&gt;{icon}&lt;/i&gt;
    &lt;/&gt;
  );
};

export const App = () =&gt; {
  return (
    &lt;div&gt;
      &lt;ButtonWithIconExtended color='green' icon='add' text='Add' /&gt;
      &lt;ButtonWithIconExtended color='red' icon='delete' text='Delete' /&gt;
      &lt;ButtonWithIconExtended color='blue' icon='edit' text='Edit' /&gt;
    &lt;/div&gt;
  );
};
</code></pre></div></div> <p>Após refatorar o código do botão, teremos um componente que será não só menor e mais legível, como também permitirá maior flexibilidade na sua implementação.</p> <h3 id="lliskov-substitution-">L — Liskov Substitution <a name="chapter-3.3"></a></h3> <p>Essa explicação pode ser um pouco confusa, mas já te explico como que funciona.</p> <p><code class="language-plaintext highlighter-rouge">Se um componente do tipo S é uma representação do componente T, então componentes do tipo T devem poder ser substituidos pelo componente S sem alterar o funcionamento desejado da aplicação.</code></p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/59llpurlycdli96e909k.png" alt="Liskov Substitution"/></p> <p>Basicamente, se você está criando a sua própria versão de um elemento, por exemplo, um <code class="language-plaintext highlighter-rouge">input</code> então o seu novo componente deve ser capaz de substituir um input comum e continuar funcionando.</p> <p>Vamos ver na prática com este exemplo <code class="language-plaintext highlighter-rouge">errado</code> antes de refatorar:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type PrettyInputProps = {
  isLarge: boolean;
};

const PrettyInput = ({ isLarge }: PrettyInputProps) =&gt; (
  &lt;div
    style=
  &gt;
    &lt;input type='text' style= /&gt;
  &lt;/div&gt;
);
</code></pre></div></div> <p>Este componente JAMAIS poderia substituir verdadeiramente um input, pois se você tentar fazer isso daqui: <code class="language-plaintext highlighter-rouge">&lt;PrettyInput type="button" onChange={someFunction}/&gt;</code></p> <p>Isso nunca funcionaria pois seu componente desconhece tais propriedades que são intrísecas ao <code class="language-plaintext highlighter-rouge">HTMLInput</code> comum.</p> <p>Para resolver isto é bem simples, basta extender seu componente para herdar as propriedades de um <code class="language-plaintext highlighter-rouge">HTMLInput</code> e repassar essas <code class="language-plaintext highlighter-rouge">props</code> para o devido input:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>interface PrettyInputProps extends React.HTMLAttributes&lt;HTMLInputElement&gt; {
  isLarge: boolean;
}

const PrettyInput = ({ isLarge, ...rest }: PrettyInputProps) =&gt; (
  &lt;div
    style=
  &gt;
    &lt;input type='text' style= {...rest} /&gt;
  &lt;/div&gt;
);

export const App = () =&gt; (
  &lt;div&gt;
    &lt;PrettyInput isLarge={true} placeholder='Type something...' /&gt;
    &lt;PrettyInput isLarge={false} onChange={console.log} /&gt;
    &lt;PrettyInput isLarge={false} onLoad={console.log} /&gt;
  &lt;/div&gt;
);

</code></pre></div></div> <h3 id="iinterface-segregation-">I — Interface Segregation <a name="chapter-3.4"></a></h3> <p>Componentes não deveriam depender de <code class="language-plaintext highlighter-rouge">props</code> que eles não utilizam</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/p11bo1hoxqgsv7ssbk2l.png" alt="Interface Segregation"/></p> <p>Veja o seguinte exemplo:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type SimpleCardProps = {
  user: {
    fullName: string;
    firstName: string;
    lastName: string;
    state: string;
    country: string;
    cellphone: string;
    email: string;
    avatar: string;
    description: string;
  };
};

const Avatar = ({ user }: SimpleCardProps) =&gt; (
  &lt;div
    style=
  &gt;
    &lt;img src={user.avatar} alt='description' /&gt;
  &lt;/div&gt;
);

const SimpleCard = ({ user }: SimpleCardProps) =&gt; {
  return (
    &lt;div&gt;
      &lt;h2&gt;{user.fullName}&lt;/h2&gt;
      &lt;Avatar user={user} /&gt;
      &lt;p&gt;{user.description}&lt;/p&gt;
    &lt;/div&gt;
  );
};
</code></pre></div></div> <p>Este exemplo é mais ingênuo, contudo esta necessidade se torna mais evidente em componentes maiores e que se utilizam de muitos dados.</p> <p>Basicamente, o componente de Avatar não tem nenhuma necessidade de entender outros detalhes do usuário, desde que ele utiliza apenas a URL da imagem, ele nem mesmo deveria conhecer o tipo do usuário.</p> <p>Segue o exemplo refatorado:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type SimpleCardProps = {
  user: {
    fullName: string;
    firstName: string;
    lastName: string;
    state: string;
    country: string;
    cellphone: string;
    email: string;
    avatarUrl: string;
    description: string;
  };
};

const Avatar = ({ avatarUrl }: { avatarUrl: string }) =&gt; (
  &lt;div
    style=
  &gt;
    &lt;img src={avatarUrl} alt='description' /&gt;
  &lt;/div&gt;
);

const SimpleCard = ({ user }: SimpleCardProps) =&gt; {
  return (
    &lt;div&gt;
      &lt;h2&gt;{user.fullName}&lt;/h2&gt;
      &lt;Avatar avatarUrl={user.avatarUrl} /&gt;
      &lt;p&gt;{user.description}&lt;/p&gt;
    &lt;/div&gt;
  );
};
</code></pre></div></div> <p>Após uma simples refatoração, podemos ver que o componente de Avatar agora não depende do tipo do usuário, apenas necessita de receber uma URL, podendo ser utilizado em outros contextos, diminuindo o acomplamento com o Card de usuário.</p> <h3 id="ddependency-inversion-">D — Dependency Inversion <a name="chapter-3.5"></a></h3> <p>Componetes genéricos devem depender de abstrações e não de implementações.</p> <p>Os detalhes de implementação devem ser de responsabilidade do componente pai.</p> <p><img src="https://dev-to-uploads.s3.amazonaws.com/uploads/articles/lmi29yz4k5tkhbnk7a0u.png" alt="Dependency Inversion"/></p> <p>Para este exemplo vamos começar com um componente de formulário simples:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>const SimpleSimpleForm = () =&gt; {
  const [username, setUsername] = React.useState('');
  const [password, setPassword] = React.useState('');
  const [error, setError] = React.useState('');
  const [loading, setLoading] = React.useState(false);

  const handleSubmit = (e: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {
    e.preventDefault();
    setLoading(true);
    setError('');

    fetch('https://reqres.in/api/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: username, password }),
    })
      .then((res) =&gt; res.json())
      .then((res) =&gt; {
        setLoading(false);
        if (res.error) {
          setError(res.error);
          return;
        }
        console.log(res);
      })
      .catch((err) =&gt; {
        setLoading(false);
        setError(err.message);
      });
  };

  if (loading) {
    return &lt;div&gt;Loading...&lt;/div&gt;;
  }

  if (error) {
    return (
      &lt;div&gt;
        {error}
        &lt;button onClick={() =&gt; setError('')}&gt;Reset&lt;/button&gt;
      &lt;/div&gt;
    );
  }

  return (
    &lt;form onSubmit={handleSubmit}&gt;
      &lt;div&gt;
        &lt;label htmlFor='username'&gt;Username&lt;/label&gt;
        &lt;input
          type='text'
          id='username'
          value={username}
          onChange={(e) =&gt; setUsername(e.target.value)}
        /&gt;
      &lt;/div&gt;

      &lt;div&gt;
        &lt;label htmlFor='password'&gt;Password&lt;/label&gt;
        &lt;input
          type='password'
          id='password'
          value={password}
          onChange={(e) =&gt; setPassword(e.target.value)}
        /&gt;
      &lt;/div&gt;

      &lt;button type='submit'&gt;Login&lt;/button&gt;
    &lt;/form&gt;
  );
};
</code></pre></div></div> <p>Num cenário onde temos que reutilizar este componente em diversos locais da aplicação, mas a depender do local, a função de submit vai precisar ser alterada.</p> <p>O objetivo agora é tornar esse componente se tornar mais genérico, fazendo com que o componente pai seja responsável por lidar com a requisição e seu retorno.</p> <p>Agora veja o componente após a refatoração:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type SimpleSimpleFormProps = {
  onSubmit: (username: string, password: string) =&gt; void;
};

const SimpleSimpleForm = ({ onSubmit }: SimpleSimpleFormProps) =&gt; {
  const [username, setUsername] = React.useState('');
  const [password, setPassword] = React.useState('');

  const handleSubmit = (e: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {
    e.preventDefault();
    onSubmit(username, password);
  };

  return (
    &lt;form onSubmit={handleSubmit}&gt;
      &lt;div&gt;
        &lt;label htmlFor='username'&gt;Username&lt;/label&gt;
        &lt;input
          type='text'
          id='username'
          value={username}
          onChange={(e) =&gt; setUsername(e.target.value)}
        /&gt;
      &lt;/div&gt;

      &lt;div&gt;
        &lt;label htmlFor='password'&gt;Password&lt;/label&gt;
        &lt;input
          type='password'
          id='password'
          value={password}
          onChange={(e) =&gt; setPassword(e.target.value)}
        /&gt;
      &lt;/div&gt;

      &lt;button type='submit'&gt;Login&lt;/button&gt;
    &lt;/form&gt;
  );
};

export const App = () =&gt; {
  const [user, setUser] = React.useState&lt;null | unknown&gt;(null);

  const handleLoginWithRestAPI = async (username: string, password: string) =&gt; {
// details
  };

  const handleLoginWithGraphQL = async (username: string, password: string) =&gt; {
// details
  };

  return (
    &lt;&gt;
      &lt;h1&gt;Login With Provider 1&lt;/h1&gt;
      &lt;SimpleSimpleForm onSubmit={handleLoginWithRestAPI} /&gt;

      &lt;h1&gt;Login With Provider 2&lt;/h1&gt;
      &lt;SimpleSimpleForm onSubmit={handleLoginWithGraphQL} /&gt;

      &lt;h1&gt;User&lt;/h1&gt;
      &lt;pre&gt;{JSON.stringify(user, null, 4)}&lt;/pre&gt;
    &lt;/&gt;
  );
};

</code></pre></div></div> <p>Após essa refatoração é notavel como podemos delegar para o componente pai os detalhes de implementação sobre como o método de login é realizado.</p> <h2 id="bônus-ocp--dip">Bônus: OCP + DIP</h2> <p>O verdadeiro potencial dos princípios SOLID mora não só na sua adoção e compreensão, mas também na capacidade de interligar um princípio com outro quando temos a oportunidade.</p> <p>Neste exemplo bônus eu vou juntar 2 deles: Open-Close Principle com Dependency Inversion Principle.</p> <p>O objetivo é:</p> <ul> <li>Tornar o componente de formulário extensível para que seus campos sejam customizáveis;</li> <li>Tornar este componente genérico para que seu componente pai lide com os detalhes de implementação do submit.</li> </ul> <p>Como resultado, teremos o seguinte código:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type SimpleSimpleFormProps = {
  onSubmit: (event) =&gt; void;
  children: React.ReactNode;
};

const SimpleSimpleForm = ({ onSubmit, children }: SimpleSimpleFormProps) =&gt; {
  const handleSubmit = (event: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {
    event.preventDefault();
    onSubmit(event);
  };

  return (
    &lt;form onSubmit={handleSubmit}&gt;
      {children}

      &lt;button type='submit'&gt;Login&lt;/button&gt;
    &lt;/form&gt;
  );
};

export const App = () =&gt; {
  const handleLogin = async (event: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {
    const username = event.currentTarget.username.value;
    const password = event.currentTarget.password.value;


    // details
  };

  const handleRegister = async (event: React.FormEvent&lt;HTMLFormElement&gt;) =&gt; {
    const username = event.currentTarget.username.value;
    const password = event.currentTarget.password.value;
    const passwordConfirmation = event.currentTarget.passwordConfirmation.value;
    const email = event.currentTarget.email.value;

    // details
  };

  return (
    &lt;&gt;
      &lt;h1&gt;Login With Provider 1&lt;/h1&gt;
      &lt;SimpleSimpleForm onSubmit={handleLogin}&gt;
        &lt;input type='text' placeholder='Username' name='username' /&gt;
        &lt;input type='password' placeholder='Password' name='password' /&gt;
      &lt;/SimpleSimpleForm&gt;

      &lt;h1&gt;Register Form&lt;/h1&gt;
      &lt;SimpleSimpleForm onSubmit={handleRegister}&gt;
        &lt;input type='text' placeholder='Username' name='username' /&gt;
        &lt;input type='password' placeholder='Password' name='password' /&gt;
        &lt;input
          type='passwordConfirmation'
          placeholder='Confirm Password'
          name='confirm'
        /&gt;
        &lt;input type='text' placeholder='Email' name='email' /&gt;
      &lt;/SimpleSimpleForm&gt;
    &lt;/&gt;
  );
};
</code></pre></div></div> <p>Note que o ponto onde parei ainda está longe do ideal:</p> <ul> <li>O componente pai recebe seus campos a partir do evento de submit e não temos compo inferir tipos nisso <em>ainda</em>;</li> <li>Não temos tratamento de erros;</li> </ul> <p>Apesar da implementação estar longe de ser ideal e poder evoluir bastante ainda, o principal é cultivar a ideia de fundir 2 princípios SOLID para tornar o componente o mais genérico e extensível possível.</p> <h2 id="conclusão">Conclusão</h2> <p>Design de software pode ser algo bem abstrato e complexo de entender a princípio, mas é que naturalmente iremos enxergar a necessidade de utilizar.</p> <p>Tenho certeza que esses princípios são de grande utilidade no desenvolvimento Frontend, especialmente nas aplicações ReactJS onde nós somos responsáveis por todas as decisões na aplicação, desde de pacotes e funcionamento, até arquitetura e estrutura de pastas que não são a mesma coisa 😂.</p> <h2 id="referências">Referências</h2> <p>Titulo: The S.O.L.I.D Principles in Pictures Autor: Ugonna Thelma May 18, 2020 https://medium.com/backticks-tildes/the-s-o-l-i-d-principles-in-pictures-b34ce2f1e898</p> <p>Titulo: 10 Best Practices for Writing Clean React Code Autor: Dhawal Pandya https://www.turing.com/kb/writing-clean-react-code</p> <p>Titulo: 7 React Clean Code Tips You Should Know Autor: Juntao Qiu https://itnext.io/7-react-clean-code-tips-you-should-know-846b8108fc46</p> <p>Titulo: How to Write Cleaner React Code Autor: Reed Barger https://www.freecodecamp.org/news/how-to-write-cleaner-react-code/</p> <p>Titulo: 10 Must-Know Patterns for Writing Clean Code with React and TypeScript✨🛀 Autor: Alex Omeyer https://dev.to/alexomeyer/10-must-know-patterns-for-writing-clean-code-with-react-and-typescript-1m0g</p> <p>Titulo: Four Tips for Writing Clean React Code Autor: Julien Delange https://www.codiga.io/blog/clean-react-code/</p> <p>Titulo: This is the Only Right Way to Write React clean-code - SOLID Autor: Islem Maboud https://www.youtube.com/watch?v=MSq_DCRxOxw</p>]]></content><author><name></name></author><category term="frontend"/><category term="solid"/><category term="frontend"/><category term="reactjs"/><category term="typescript"/><category term="javascript"/><summary type="html"><![CDATA[Como usar os princípios SOLID no frontend para criar aplicações escaláveis e de fácil manutenção.]]></summary></entry></feed>